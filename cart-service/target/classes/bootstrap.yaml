spring:
  application:
    name: cart-service
  profiles:
    active: windows
  cloud:
    nacos:
      server-addr: 127.0.0.1
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-jdbc.yaml
          - data-id: shared-log.yaml
          - data-id: shared-swagger.yaml







